# puerto donde corre la app
PORT=3000

# URL pública/host (IMPORTANTE: incluye el protocolo y puerto)
# útil para links a las fotos y para generar QR
HOST=http://localhost:3000

# ruta del archivo de la base de datos (opcional, si tu código la lee)
DB_PATH=./db/data.db

# correo para notificaciones (en dev puedes usar Mailtrap o dejar vacío y usar fallback console)
EMAIL_USER=tu_email@example.com
EMAIL_PASS=tu_app_password_or_app_specific_password
SMTP_SERVICE=gmail
